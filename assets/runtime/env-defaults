#!/bin/bash
##WARNING: set environment variables that need to be accessible from outside this script
#in the DockerFile

#print statements as they are executed
[[ -n $DEBUG ]] && set -x

USERMAP_UID=${USERMAP_UID:-1000}
USERMAP_GID=${USERMAP_GID:-1000}
